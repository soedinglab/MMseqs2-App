# https://aka.ms/yaml

trigger:
  - master
pool:
  vmImage: 'macOS-10.14'
steps:
  - task: NodeTool@0
    inputs:
      versionSpec: '12.x'
  - script: |
      brew install unzip make
      npm install
      npm run electron:deps
      npm run electron:build
